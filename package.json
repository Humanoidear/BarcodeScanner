{
    "name": "DANAscan",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
        "deploy": "cd frontend && npm install && cd .. && cd backend && npm install && cd .. && cd frontend && npm run build && cd .. && cd backend && node index.js"
    }
}
