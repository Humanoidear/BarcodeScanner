version: '3'

services:
  postgres:
    image: postgres:14-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:2345:5432"
    env_file:
      - .env

volumes:
  pgdata: